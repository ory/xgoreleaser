# AUTO-GENERATED, DO NOT EDIT!
# Please edit the original at https://github.com/ory/meta/blob/master/templates/repository/common/.github/workflows/licenses.yml

name: Licenses

on:
  pull_request:
  push:
    branches:
      - main
      - v3
      - master

jobs:
  licenses:
    name: License compliance
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Install script
        uses: ory/ci/licenses/setup@99c3afad6f2bcb8e8f557fd1abe9d0044e884b69 # master
        with:
          token: ${{ github.token }}
      - name: Check licenses
        uses: ory/ci/licenses/check@99c3afad6f2bcb8e8f557fd1abe9d0044e884b69 # master

  licenses-write:
    name: Write license report
    if: ${{ github.event_name == 'push' && github.ref_protected }}
    needs: licenses
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Install script
        uses: ory/ci/licenses/setup@99c3afad6f2bcb8e8f557fd1abe9d0044e884b69 # master
        with:
          token: ${{ secrets.ORY_BOT_PAT || github.token }}
      - name: Write, commit, push licenses
        uses: ory/ci/licenses/write@99c3afad6f2bcb8e8f557fd1abe9d0044e884b69 # master
        with:
          author-email:
            ${{ secrets.ORY_BOT_PAT &&
            '60093411+ory-bot@users.noreply.github.com' ||
            format('{0}@users.noreply.github.com', github.actor) }}
          author-name: ${{ secrets.ORY_BOT_PAT && 'ory-bot' || github.actor }}
